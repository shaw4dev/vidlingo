# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Local assistant/tool state
.claude/

# iOS / Xcode
ios/build/
ios/DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Node (tooling)
node_modules/

# Local dev database
*.db
*.sqlite3

# Data / media (never commit content or large media)
# content/packages/ is the exported corpus (app.content.packages) - it is how the
# library reaches a production DB, but it's caption text from third-party videos
# and is regenerable, so it moves out of band rather than through the repo.
content/packages/
*.mp4
*.mov
*.wav
*.m4a
data/
content/raw/
