# Copy to .env and fill in. Never commit .env.
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/vidlingo

# Auth — generate a strong random value in prod, e.g. `openssl rand -hex 32`
# SECRET_KEY=
# ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ASR_API_KEY=
# LLM_API_KEY=
